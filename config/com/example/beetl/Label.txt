<%
	// 标签
	// 所谓标签，即允许处理模板文件里的一块内容，功能等于同jsp tag。
	
	layout("/com/example/beetl/layout.html",{title:'主题',name:'陈俊'}){
	// layoutContent 是layout标签体{}渲染后的结果
%>
 Hello,this is main part
<%
	// layoutContent 是layout标签体{}渲染后的结果
	}
%>
<%
	// Beetl内置了另外一个标签是include,允许include 另外一个模板文件
	include("/com/example/beetl/header.html"){}
	// 在标签中，{} 内容将依据标签的实现而执行，layout标签将执行{}中的内容，而include标签则忽略标签体内容。
%>