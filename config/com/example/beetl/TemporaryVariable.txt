<%
	/** 
	*	临时变量定义
	*	在模板中定义的变量成为临时变量，这类似js中采用var 定义的变量
	**/
	var a = 26;
	var b = 3, c = "chenjun", d = true, e = null;
	var f = [1,2,3];
	var g = {age : a, name : c};
	var i = a + b;
%>
g 的值为 ${g}
i 的值为${i}