<%
	for(user in list){
		println(userLP.index);// 当前的索引，从1开始
		println(user.name);
		println(userLP.size);// 集合的长度
		println(userLP.first);// 是否是第一个
		println(userLP.last);// 是否是最后一个
		println(userLP.even);// 索引是否是偶数
		println(userLP.odd);// 索引是否是奇数
	}
%>